# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ARGameRX' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ARGameRX

  target 'ARGameRXTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ARGameRXUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  pod 'RxSwift', '~> 3.2'
  pod 'RxCocoa', '~> 3.2'
end
